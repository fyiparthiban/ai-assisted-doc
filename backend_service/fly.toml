app = "practice-ui-backend-service"
primary_region = "lax"

[build]
  dockerfile = "Dockerfile"

[env]
  BACKEND_PORT = "8003"

[http_service]
  internal_port = 8003
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]